<div class='form-banner' style="background-image: linear-gradient(-200deg, rgba(0,0,0,0.3) 5%, rgba(0,0,0,0.1) 100%), url(http://s3-eu-west-1.amazonaws.com/fovento/images/workers2.jpg);">
  <!-- <div class='thumbnail'> -->
  <div class='form-box padded' style="max-width: 500px;">
    <div class='background'>
      <div class='form text-center'>

        <h2>Inscription</h2>
        <br />
        <%= render "devise/shared/social_logins" %>


        <br />
        <p>Pour une expérience Fovento optimale, notre équipe recommande de se connecter via LinkedIn.</p>
        <p>Sans compte LinkedIn, il est aussi possible de créer un compte propre à Fovento:</p>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'form-horizontal'}) do |f| %>
        <%= f.error_notification %>
        <div class="form-inputs">
          <%= f.input :email, required: true, autofocus: true %>
          <%= f.input :password, required: true, label: "Mot de passe", hint: ("#{@minimum_password_length} caractères minimum" if @minimum_password_length) %>
          <%= f.input :password_confirmation, required: true, label: "Confirmation du mot de passe" %>
          <%= f.input :first_name, required: true, label: "Prénom" %>
          <%= f.input :last_name, required: true, label: "Nom de famille" %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Je m'inscris" %>
        </div>
        <% end %>
        <br />
        <div class="form_options">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>

