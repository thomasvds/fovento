<h2>Gestion des associations</h2>
<p> Crée et édite ici les profils des associations présentes sur la plate-forme.</p>
<table class='table table-condensed table-hover table-striped  dashboard-table'>

<%= link_to "Enregistrer une nouvelle association", new_nonprofit_profile_path, class: 'btn btn-default btn-xs' %>

<br>
<br>
  <thead>
    <th>Nom</th>
    <th>Contact</th>
    <th>Nombre de missions</th>
    <th>Actions</th>
  </thead>

  <tbody>
    <% nonprofit_profiles.each do |profile| %>
    <tr>
      <td><%= profile.name %></td>
      <td><%= profile.email %></td>
      <td><%= profile.missions.count %></td>
      <td>
        <%= link_to edit_nonprofit_profile_path(profile) do%>
        <i class="fa fa-pencil actionicon"></i>
        <% end %>
        &nbsp;
        &nbsp;
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<!-- ACTION ICONS LEGEND -->
<i class="fa fa-pencil infoicon"></i><small>   Editer le profil</small> &nbsp;
